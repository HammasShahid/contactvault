spring:
  application:
    name: contactvault
  profiles:
    active: dev

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate
  jwt:
    accessTokenExpirationMillis: 3600000 # 2 Hours
    secret: my-secret-my-secret-my-secret-my-secret
